# MomenPair 行动清单

更新日期：2026-08-18

本文将 `Baseline.md` 转换为可执行任务。默认产品形态是仅受邀家庭成员访问的封闭家庭空间，客户端和后端继续保持代码与依赖分离。

## 标记说明

- `P0`：首版上线必需。
- `P1`：P0 稳定后完成。
- `P2`：后续增强。
- `BLOCKER`：未完成时会阻塞后续任务。
- `DECISION`：需要产品或技术负责人明确选择并记录结论。

## 当前状态

- [x] Flutter 3.47.0、Android、Xcode、CocoaPods 工具链配置完成。
- [x] `flutter analyze` 无问题，Flutter 基础测试通过。
- [x] Docker Desktop、代理和 Docker Hub 拉取验证完成。
- [x] 客户端五页导航、主题、动画和本地化框架完成。
- [x] FastAPI、MySQL、Redis、MinIO、Alembic 与基础测试框架完成。
- [x] 用户、登录身份、家庭、家庭成员基础模型与首个迁移完成。
- [x] 放弃手机号与短信验证码，只保留相互独立的微信账号和 QQ 账号。
- [x] 账号模型增加“一名用户只有一个第三方登录身份”的数据库约束。
- [x] 微信/QQ 独立账号的 Fake 登录、令牌轮换、会话恢复与退出闭环完成。
- [x] 家庭创建、一次性邀请、加入、成员角色、移除和退出闭环完成。
- [x] 本地完整 Compose 服务启动并通过就绪检查。
- [x] 小程序端工程基线：五页导航、自定义 tabBar、玻璃与动画令牌、键值化中英文案、微信登录闭环与 41 个单元测试。
- [x] macOS arm64 开发构建及 Keychain/网络签名权限验证通过。
- [x] iOS Debug 无签名设备构建及 Keychain entitlement 编译验证通过。
- [ ] 客户端分别在 Android、iOS 上完成一次启动冒烟测试。
- [ ] Windows 在真实 Windows 环境完成工程生成与冒烟测试。

## 接下来立即执行

按顺序完成，后项默认依赖前项。

- [ ] `P0` 在仓库根目录创建 `.env`，替换所有示例密码和密钥。
- [ ] `P0` 执行 `docker compose pull` 和 `docker compose up --build -d`。
- [x] `P0` 执行 `docker compose exec api alembic upgrade head`。
- [x] `P0` 验证 `/api/v1/health/live`、`/api/v1/health/ready` 和 `/docs`。
- [ ] `P0` 创建 MinIO 私有桶 `momenpair-media`，禁止匿名读写。
- [ ] `BLOCKER` 分别在微信开放平台和 QQ 互联控制台核实当前个人主体能否申请移动应用及网站应用；如不允许，记录为上线硬阻塞，不使用非正式凭据绕过审核。
- [ ] `BLOCKER` 确认产品正式中文名、英文名、Logo、官网域名、隐私政策 URL 和用户协议 URL。
- [ ] `BLOCKER` 确认域名、HTTPS 证书、ICP备案及开放平台回调域名是否已准备完成。
- [ ] `DECISION` 固定四端应用标识，不再随意修改。
- [x] `P0` 实现客户端环境配置、HTTP 网络层和安全存储。
- [ ] `P0` 并行验证微信和 QQ 登录最小闭环，先完成能够取得有效测试资质的平台。

建议应用标识在确认商标和组织域名后填写：

| 平台 | 待确认标识 |
| --- | --- |
| Android | `applicationId`，例如 `com.example.momenpair` |
| iOS | Bundle ID |
| macOS | Bundle ID，可与 iOS 同属一个 App Group 体系 |
| Windows | Package Identity / 协议回调 Scheme |
| 通用回调 | HTTPS OAuth 回调域名与自定义 URL Scheme |

## M0：本地环境与工程基线

### 后端与基础设施

- [ ] `P0` 验证 MySQL 8.4 使用 `utf8mb4`、严格模式和 ROW 格式 binlog。
- [ ] `P0` 验证 Redis 持久化、健康检查和本地访问限制。
- [ ] `P0` 创建 MinIO 桶初始化脚本或应用启动检查，重复执行必须幂等。
- [ ] `P0` 为开发、测试、预发布、生产定义独立配置，禁止共享数据库和密钥。
- [ ] `P0` 增加请求 ID、中间件日志、结构化错误和全局异常处理。
- [ ] `P0` 增加数据库迁移 CI 检查和迁移离线 SQL 检查。
- [ ] `P0` 为 Docker 镜像固定可复现依赖，保留 `uv.lock`。
- [ ] `P1` 增加服务指标、链路追踪和错误告警。

### 客户端

- [ ] `P0` 在 Android 模拟器启动并检查五页导航、深色模式和中文文案。
- [ ] `P0` 在真实 iPhone 启动并检查安全区域、动画和系统返回行为。
- [ ] `P0` 在 macOS 启动并检查侧边导航、窗口缩放和键盘操作。
- [x] `P0` 通过 `API_BASE_URL` 编译变量集中配置各环境地址，业务代码无散落硬编码 URL。
- [x] `P0` 接入安全存储，刷新令牌只保存到 Keychain/Keystore/Credential Locker。
- [x] `P0` 选择并封装 HTTP 客户端，完成 JSON、稳定错误码和认证接口处理；统一重试、请求 ID 与普通业务请求鉴权刷新仍待实现。
- [ ] `P0` 增加路由、依赖注入和领域状态管理方案，先做最小验证再锁定依赖。
- [ ] `P0` 增加网络断开、会话过期和服务不可用的本地化错误页面。

## M1：独立社交账号与会话

### 已确认的账号边界

- [x] 登录平台只允许 `wechat` 和 `qq`，不实现手机号、密码或短信验证码登录。
- [x] 一个 MomenPair 用户只能有一个第三方登录身份。
- [x] 同一个第三方身份只能对应一个 MomenPair 用户。
- [x] 微信登录和 QQ 登录的家庭关系、内容、通知及个人资料完全隔离。
- [x] 不提供增加绑定、换绑、解绑、跨平台合并或人工改绑。
- [x] `P0` 登录页明确提示：改用另一个平台会进入另一个独立账号，不能访问当前账号数据。

### 登录 API

建议接口：

| 方法 | 路径 | 用途 |
| --- | --- | --- |
| `POST` | `/api/v1/auth/wechat/mobile` | 使用微信移动端一次性 code 登录或注册 |
| `POST` | `/api/v1/auth/qq/mobile` | 使用 QQ 移动端一次性 code 登录或注册 |
| `POST` | `/api/v1/auth/oauth/desktop/transactions` | 创建桌面浏览器 OAuth 登录事务 |
| `POST` | `/api/v1/auth/refresh` | 轮换刷新令牌 |
| `POST` | `/api/v1/auth/logout` | 退出当前设备 |
| `POST` | `/api/v1/auth/logout-all` | 退出全部设备 |

- [ ] `P0` OAuth 登录事务使用高熵随机 `state`、短有效期和一次性消费。
- [ ] `P0` 第三方临时 code 只能交换一次，重复提交返回稳定错误。
- [ ] `P0` 服务端验证 token 受众、AppID、有效期和第三方响应来源。
- [ ] `P0` 登录接口按 IP、设备和第三方供应商结果限流。
- [x] `P0` 首次成功授权时在同一事务创建用户和唯一登录身份。
- [x] `P0` 已存在的 `provider + provider_subject` 只能登录其原用户。
- [x] `P0` 数据库同时保留 `user_id` 以及 `provider + provider_subject` 唯一约束。
- [ ] `P0` 覆盖 state 不匹配、code 过期/重放、伪造 AppID、回调劫持和并发首次登录测试。

### 令牌与会话

- [x] `P0` 实现短时访问令牌，默认 15 分钟。
- [x] `P0` 实现不透明的轮换刷新令牌，服务端只保存摘要。
- [x] `P0` 刷新令牌按设备记录并支持撤销、单设备退出和全部退出。
- [x] `P0` 检测刷新令牌重放；发现旧令牌复用时撤销对应令牌家族。
- [x] `P0` 家庭成员退出或被移除后，下一次家庭资源请求必须立即失效。
- [ ] `P0` 客户端实现并发请求下的单次刷新锁，避免刷新风暴。
- [ ] `P0` 安全日志不记录访问令牌、刷新令牌、OAuth code、第三方 token 或完整第三方身份标识。

## M2：微信登录

### 接入策略

- Android/iOS：微信开放平台“移动应用”登录，客户端调用官方原生 SDK，后端用临时 `code` 换取身份信息。
- macOS/Windows：微信开放平台“网站应用”扫码 OAuth，通过系统浏览器和 HTTPS 回调完成。
- Flutter：先评估维护活跃的插件；若无法覆盖 Flutter 3.47、SwiftPM、Android 新版本和隐私清单，则在仓库内使用 Platform Channel 封装官方 SDK。
- AppSecret 和微信 access token 只能存在于后端，客户端只传一次性 `code`、`state` 和本应用登录事务 ID。

官方入口：

- [微信开放平台](https://open.weixin.qq.com/)
- [微信开放平台开发文档](https://developers.weixin.qq.com/doc/oplatform/)

### 申请与配置

- [ ] `BLOCKER` 完成微信开放平台开发者资质认证。
- [ ] `BLOCKER` 创建移动应用并通过审核。
- [ ] `BLOCKER` 创建网站应用并通过审核，用于 macOS/Windows 扫码登录。
- [ ] `BLOCKER` 准备官网、隐私政策、应用图标、应用介绍和可访问的 HTTPS 回调域名。
- [ ] `P0` 在开放平台登记 Android 包名和正式签名信息。
- [ ] `P0` 在开放平台登记 iOS Bundle ID、Universal Link 等控制台要求的信息。
- [ ] `P0` 为开发、预发布和生产设计可审核的应用/回调隔离方案。
- [ ] `P0` 将同一环境的移动与网站应用归入可获得稳定 UnionID 的开放平台关系；无法获得时禁止跨 AppID 推断为同一微信账号。

### 后端

- [x] `P0` 增加 `POST /api/v1/auth/wechat/mobile` 及可替换适配器；真实微信换票仍待开放平台凭据。
- [ ] `P0` 增加桌面扫码登录事务创建、OAuth 回调和一次性结果领取接口。
- [ ] `P0` 对 OAuth `state`、事务有效期、回调来源和一次性消费做校验。
- [ ] `P0` 服务端与微信交换 token，禁止客户端持有 AppSecret。
- [ ] `P0` 使用 `provider + provider_subject` 唯一约束关联登录身份。
- [ ] `P0` 优先保存 UnionID；若不可用，明确记录 AppID 范围内的 OpenID，禁止跨应用误认为同一微信身份。
- [ ] `P0` 首次微信授权创建独立用户，后续相同稳定身份只登录该用户。
- [ ] `P0` 微信流程不暴露绑定、解绑或合并入口。
- [ ] `P0` 覆盖取消授权、code 过期、code 重放、微信未安装、回调失败和并发首次登录测试。

### 客户端

- [ ] `P0` 定义统一 `SocialAuthProvider` 接口。
- [ ] `P0` Android/iOS 实现微信 SDK 可用性检查、拉起、取消和回调处理。
- [ ] `P0` macOS/Windows 使用系统浏览器显示扫码授权页，不在应用内保存微信网页登录凭据。
- [ ] `P0` Universal Link、自定义 Scheme 和桌面回调必须防止其他应用劫持登录结果。
- [ ] `P0` 用户拒绝授权或未安装微信时提供可恢复提示；可选择 QQ 登录，但明确 QQ 会进入独立账号。

## M3：QQ 登录

### 接入策略

- Android/iOS：QQ 互联移动应用 SDK。
- macOS/Windows：QQ 互联网站应用 OAuth，通过系统浏览器完成授权。
- 服务端以 `AppID + OpenID` 对 QQ 身份进行稳定关联，不使用昵称或 QQ 号作为账号主键。
- App Key、AppSecret 和第三方 access token 只能保存在后端。

官方入口：

- [QQ 互联](https://connect.qq.com/)
- [QQ 互联开发文档](https://wiki.connect.qq.com/)

### 申请与配置

- [ ] `BLOCKER` 完成 QQ 互联开发者认证。
- [ ] `BLOCKER` 创建并审核移动应用。
- [ ] `BLOCKER` 创建并审核网站应用，用于桌面端浏览器 OAuth。
- [ ] `P0` 登记 Android 包名、签名和回调 Scheme。
- [ ] `P0` 登记 iOS Bundle ID、Universal Link/URL Scheme 等控制台要求的信息。
- [ ] `P0` 登记桌面 OAuth HTTPS 回调域名。
- [ ] `P0` 配置开发协作者或测试账号，避免测试阶段对全部用户开放。

### 实现

- [x] `P0` 增加 `POST /api/v1/auth/qq/mobile` 及可替换适配器；真实 QQ 换票仍待开放平台凭据。
- [ ] `P0` 增加桌面 QQ OAuth 事务、回调和一次性结果领取接口。
- [ ] `P0` 校验 `state`、code、token 受众、AppID 和有效期。
- [ ] `P0` 首次 QQ 授权创建独立用户，后续相同稳定身份只登录该用户。
- [ ] `P0` Flutter 封装 QQ 登录可用性、拉起、取消、回调和错误映射。
- [ ] `P0` QQ 流程不暴露绑定、解绑或合并入口。
- [ ] `P0` 覆盖 QQ 未安装、用户取消、网络失败、token 过期、回调劫持和并发首次登录测试。

## M4：账号资料与生命周期

- [x] `P0` 登录身份枚举仅保留微信和 QQ，并增加单用户单身份迁移。
- [x] `P0` 个人资料接口只读返回当前登录平台，不返回可用于关联账号的原始第三方标识。
- [x] `P0` 客户端“我的”页面不显示绑定、换绑、解绑或账号合并入口。
- [ ] `P0` 在帮助文档说明账号无法跨平台恢复，优先使用微信或 QQ 官方账号找回。
- [ ] `P0` 实现头像签名上传、服务端文件头校验、重新编码、缩略图和 EXIF 清理。
- [ ] `P0` 实现昵称修改频率限制及格式校验。
- [ ] `P0` 实现单设备退出、全部设备退出、数据导出和账号注销。
- [ ] `P0` 注销定义家庭创建者、家庭仅剩一人和待处理邀请等边界行为。

## M5：家庭空间与权限

- [x] `P0` 实现创建家庭；首个创建者成为管理员。
- [x] `P0` 实现一次性邀请码，包含有效期、次数和撤销状态；邀请链接待官网域名确认后增加。
- [x] `P0` 实现接受邀请，禁止一个账号同时加入多个家庭。
- [x] `P0` 实现成员列表、退出、管理员移除和角色变更。
- [x] `P0` 最后一名管理员在仍有其他成员时必须先转让角色；家庭仅剩自己时允许退出并撤销邀请。
- [ ] `P0` 所有家庭资源表包含 `family_id` 和必要复合索引。
- [x] `P0` 家庭管理查询从认证用户解析当前家庭并校验有效成员关系，不信任客户端提交的家庭 ID；后续内容接口继续沿用。
- [ ] `P0` 实现家庭成员关系缓存及变更时失效。
- [ ] `P0` 对跨家庭读写、猜测对象 ID、已退出成员令牌做越权测试。
- [ ] `P0` 首版所有内容固定为家庭内公开，API 拒绝客户端写入预留的“仅自己”状态。

## M6：日志、注意、倒数与互动

### 数据模型与 API

- [ ] `P0` 设计日志、注意、倒数独立业务表和共享字段。
- [ ] `P0` 设计附件、评论、回复、点赞和计数表。
- [ ] `P0` 使用游标分页，禁止深 offset 成为主要分页方式。
- [ ] `P0` 写接口支持幂等键；编辑接口使用版本号或 ETag 防止静默覆盖。
- [ ] `P0` 点赞使用数据库唯一约束，重复请求不能重复计数。
- [ ] `P0` 评论数据记录根评论与被回复评论，界面只展示两层。
- [ ] `P0` 删除内容后隐藏正文、阻止家庭成员访问并异步清理附件。

### Markdown、Mermaid 与 LaTeX

- [ ] `P0` 完成 Flutter Markdown 渲染库选型 POC。
- [ ] `P0` 完成 Mermaid 渲染 POC，评估 WebView、隔离 HTML 或服务端渲染的性能与安全。
- [ ] `P0` 完成 LaTeX 渲染 POC，验证长公式、错误公式和深色模式。
- [ ] `P0` 禁用原始 HTML、脚本、危险 URL 和 Mermaid 外部资源。
- [ ] `P0` 限制文本长度、语法树节点、渲染时间和内存占用。
- [ ] `P0` 保存原始文本与渲染协议版本，避免解析器升级后内容不可重现。

### 图片与对象存储

- [ ] `P0` 实现签名上传初始化接口和完成确认接口。
- [ ] `P0` 服务端校验真实 MIME、文件头、像素尺寸和大小。
- [ ] `P0` 重新编码 JPEG/PNG/WebP，生成多尺寸缩略图并移除 EXIF。
- [ ] `P0` 私有对象使用短时签名 URL，不能返回永久公开地址。
- [ ] `P0` 临时上传对象设置过期清理任务。
- [ ] `P0` 成员退出、内容删除后立即阻止重新签发访问 URL。

### 倒计时

- [ ] `P0` 实现事件时区、UTC 目标时间、全天事件、主题和提醒字段。
- [ ] `P0` 实现一次性及日/周/月/年重复规则。
- [ ] `P0` 明确月末、2 月 29 日、夏令时和跨时区规则并编写测试。
- [ ] `P0` 区分倒数和纪念日，到期后展示规则与服务端一致。
- [ ] `P0` 创建/编辑/删除后同步重新安排客户端本地通知。

## M7：站内通知、实时同步与异步任务

- [ ] `P0` 建立通知表，保存类型、家庭、发起者、目标、已读时间和安全快照。
- [ ] `P0` 建立 Outbox 表，业务数据与事件在同一 MySQL 事务提交。
- [ ] `P0` 首版实现独立 Outbox Worker，按事件 ID 幂等处理并支持失败重试。
- [ ] `P0` 失败超过阈值进入死信状态并告警，禁止无限热循环。
- [ ] `P0` 实现 SSE 连接、鉴权、心跳、游标、断线重连和补偿拉取。
- [ ] `P0` 实时事件仅携带家庭 ID、资源 ID、版本和事件 ID，不携带正文。
- [ ] `P0` 家庭成员变化时立即关闭或降权旧连接。
- [ ] `P0` 点赞通知聚合，评论/回复/邀请与安全事件独立展示。
- [ ] `P0` 实现未读数、单条已读和全部已读接口。

## M8：系统推送

### Apple

推荐 iOS/macOS 直接接入 APNs Token Authentication，服务端安全保存 `.p8`、Key ID 和 Team ID。参见[Apple APNs Token 官方说明](https://developer.apple.com/documentation/usernotifications/establishing-a-token-based-connection-to-apns)。

- [ ] `BLOCKER` Apple Developer 账号和正式 Bundle ID 准备完成。
- [ ] `P0` 创建 APNs Key，私钥只下载一次并存入 Secret 管理。
- [ ] `P0` iOS/macOS 开启 Push Notifications 与所需 Background Modes。
- [ ] `P0` 客户端注册/更新设备 token，服务端按环境、设备、用户保存。
- [ ] `P0` 处理 APNs token 失效、环境不匹配、限流和过期 provider token。

### Android

- [ ] `DECISION` 中国大陆首版评估腾讯移动推送 TPNS 与华为/小米/OPPO/vivo 直连的覆盖、成本和隐私 SDK 清单。
- [ ] `P0` 原型阶段可用 FCM 验证统一推送抽象，但不能把中国大陆生产到达率建立在 FCM 单通道上。
- [ ] `P0` 定义 `PushProvider` 接口，站内通知事实不依赖任何推送供应商。
- [ ] `P0` 客户端支持通知权限、前台消息、点击跳转、token 刷新和注销。
- [ ] `P0` 推送仅携带最小事件信息，打开应用后重新鉴权获取详情。
- [ ] `P1` Windows 评估 WNS；未接入前只提供站内通知。

### 通用验收

- [ ] `P0` 系统推送按通知类型允许用户关闭，账号安全事件保留站内记录。
- [ ] `P0` 推送采用至少一次语义，客户端按事件 ID 去重。
- [ ] `P0` 覆盖在线、后台、被系统终止、token 失效、重复投递和通知关闭状态。
- [ ] `P0` 记录供应商回执和错误码，但不把推送回执当作业务已读。

## M9：小组件与本地通知

- [ ] `P1` iOS 使用 WidgetKit，实现倒计时列表和单事件小组件。
- [ ] `P1` Android 使用 App Widgets，实现等价数据展示。
- [ ] `P1` macOS 评估复用 WidgetKit。
- [ ] `P2` Windows 评估系统支持后再决定，不在首版承诺。
- [ ] `P1` 小组件只读取最小化共享数据，不保存长期刷新令牌。
- [ ] `P1` 遵守系统时间线刷新限制，不承诺后台秒级刷新。
- [ ] `P0` 时间确定的提醒优先使用本地通知，跨端事件使用远程推送。

## M10：运维后台、安全与合规

- [ ] `P0` 实现独立运维后台入口和管理身份体系。
- [ ] `P0` 管理员启用 MFA、最小权限、短会话和操作原因必填。
- [ ] `P0` 提供用户/家庭查询、成员关系排查、账号禁用和依法处置内容。
- [ ] `P0` 所有后台读取或修改家庭数据的行为写入不可抵赖审计。
- [ ] `P0` 完成隐私政策、用户协议、第三方 SDK 清单和权限用途说明。
- [ ] `P0` 首次启动不得在用户同意隐私政策前初始化非必要第三方 SDK。
- [ ] `P0` 相册、相机、通知权限只在用户触发对应功能时请求。
- [ ] `P0` 实现账号注销、数据导出和个人信息授权撤销入口。
- [ ] `P0` 建立密钥轮换、依赖扫描、漏洞修复和备份恢复流程。
- [ ] `P0` 数据库每日全量备份结合 binlog，执行真实恢复演练。
- [ ] `P0` 为 OAuth 回调滥用、code/token 重放、越权、恶意上传和刷新令牌重放建立告警。

## M11：测试、CI 与发布

### 自动化测试

- [ ] `P0` 后端领域单元测试、数据库集成测试和 API 契约测试。
- [ ] `P0` 使用真实 MySQL 测试外键、唯一约束、事务与迁移。
- [ ] `P0` Flutter 状态测试、Widget 测试和核心流程集成测试。
- [ ] `P0` 微信/QQ 登录与账号隔离、家庭邀请、退出/移除、跨家庭隔离端到端测试。
- [ ] `P0` 微信、QQ、推送全部提供可控 Fake Provider，CI 不调用真实供应商。
- [ ] `P0` 对第三方回调保留签名/状态校验契约测试和供应商沙箱冒烟测试。
- [ ] `P0` 压测登录、首页列表、点赞、评论、SSE 和通知未读数。
- [ ] `P0` 视觉回归覆盖手机/桌面、深色/浅色、中文/英文和减少动画。

### CI

- [ ] `P0` 后端执行 Ruff、Mypy、Pytest、迁移检查和依赖漏洞扫描。
- [ ] `P0` 客户端执行格式检查、`flutter analyze` 和 `flutter test`。
- [ ] `P0` Android 生成 Debug APK；macOS Runner 执行 Debug 构建。
- [ ] `P0` iOS 在无签名模式完成编译，真机签名由受控环境执行。
- [ ] `P1` Windows Runner 在 Windows CI 上构建。
- [ ] `P0` Secret 不出现在 CI 日志、缓存和构建产物中。

### 发布

- [ ] `P0` 固定应用名称、图标、启动图、包标识和商店描述。
- [ ] `P0` 配置 Android 正式签名并离线备份密钥。
- [ ] `P0` 配置 Apple Certificates、Identifiers、Profiles 和 APNs。
- [ ] `P0` 建立开发、预发布和生产服务端部署环境。
- [ ] `P0` 数据库迁移采用扩展、迁移、收缩流程并验证回滚方案。
- [ ] `P0` 服务端 API 至少兼容当前及前一个主要客户端版本。
- [ ] `P0` 建立灰度、功能开关、崩溃监控和分别紧急停用微信/QQ 登录通道的能力。

## 第三方凭据清单

以下变量名是建议命名，最终以服务端 `Settings` 实现为准。所有 Secret 只进入本地 `.env` 或生产 Secret 管理系统，绝不提交 Git。

| 类别 | 配置 |
| --- | --- |
| 微信移动应用 | AppID、AppSecret、Android/iOS 平台配置 |
| 微信网站应用 | AppID、AppSecret、HTTPS 回调域名 |
| QQ 移动应用 | AppID、App Key、Android/iOS 平台配置 |
| QQ 网站应用 | AppID、App Key、HTTPS 回调域名 |
| Apple APNs | Team ID、Key ID、`.p8` 私钥、Bundle ID、环境 |
| Android 推送 | 各厂商 App ID/Key/Secret、包名、签名指纹 |
| 对象存储 | Endpoint、Access Key、Secret Key、Bucket |

## 上线前硬性验收

- [ ] 微信和 QQ 登录入口均明确可用；某个平台不可用时提示其账号暂时无法登录，不能把另一个平台冒充为恢复方式。
- [ ] 相同第三方身份不能创建多个用户，同一用户不能拥有第二个身份，微信与 QQ 账号不会合并。
- [ ] 已退出或被移除成员不能通过旧 token、对象 ID、图片 URL 或 SSE 获取家庭数据。
- [ ] OAuth state、临时 code 和登录结果无法被重放、劫持、日志泄露或无限请求。
- [ ] Markdown、Mermaid、LaTeX 和图片无法执行脚本或访问未授权外部资源。
- [ ] 通知重复投递不会重复计数或产生重复业务副作用。
- [ ] 账号注销、数据导出、权限拒绝和第三方授权撤销均有完整流程。
- [ ] 备份已通过恢复演练，发布和数据库迁移均有可执行回滚方案。
- [ ] Android、iOS、macOS 完成真实设备/系统测试，Windows 完成独立构建验证。

## 暂不进入 P0 的范围

- 陌生人社交、关注、好友、私信和算法推荐。
- 跨家庭公开内容或全站搜索。
- 跨微信/QQ 的身份绑定、换绑、解绑、账号迁移或合并。
- 服务端跨端草稿和复杂协同编辑。
- Windows 小组件。
- 为尚未出现的规模提前拆分微服务。
