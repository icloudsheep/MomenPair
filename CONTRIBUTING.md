# 贡献指南

## 开发流程

1. 先阅读 `Baseline.md` 和 `docs/architecture.md`，确认改动没有越过产品及家庭数据边界。
2. 客户端改动限制在 `client/`，后端改动限制在 `server/`；公共契约更新放入 `docs/`。
3. 小步提交，数据库结构和 API 契约变化必须附迁移及兼容性说明。
4. 提交前运行对应端的格式化、静态检查和测试。

## 代码要求

- 不提交密钥、OAuth 临时 code、令牌、第三方身份标识或家庭内容；
- 不在 UI 代码硬编码面向用户的文案；
- 所有家庭资源查询必须包含服务端成员权限校验；
- API 错误使用稳定机器码；
- 新依赖应说明用途，锁定兼容范围，并通过安全检查；
- 新功能至少覆盖成功路径、权限失败和关键边界条件。

## 文档同步

改变运行命令、环境变量、API、数据模型或目录边界时，同步更新相应 README 和 `docs/`。产品决策发生变化时，先更新 `Baseline.md`，再调整实现。
