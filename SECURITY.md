# 安全策略

## 报告问题

当前仓库尚未配置公开安全邮箱。发现漏洞时，请通过仓库所有者提供的私密渠道报告，不要在公开 Issue 中提交利用步骤、令牌、第三方身份标识、家庭内容或其他个人数据。

报告应尽量包含受影响版本、影响范围、复现条件和建议缓解方式，但不要附带无关的真实用户数据。

## 基础要求

- 生产环境必须替换所有示例密钥和密码；
- 访问令牌、刷新令牌、OAuth 临时 code 和三方登录凭据不得写入日志；
- 家庭数据默认视为私有数据，图片不得使用永久公开 URL；
- 后端对每次家庭资源访问重新校验成员关系；
- 管理后台使用最小权限、多因素认证和操作审计；
- 依赖更新、镜像更新和数据库迁移在发布前经过安全及回滚检查。

## 支持范围

项目尚未发布稳定版本。框架阶段只接受针对当前主分支的安全修复；形成正式发布策略后，应在此文件列出仍受支持的版本。
