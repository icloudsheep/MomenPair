现在你是一个专业的前后端全栈工程师和全平台应用工程师，现在你需要从0设计一个后端，并且使用跨平台语言编写安卓、iOS、Mac、Windows应用程序。

应用程序要求实现至少以下功能：

1. 应用程序使用服务器后端的能力，需要进行用户鉴权，仅支持微信和 QQ 登录；两种登录方式对应相互独立的账号
2. 使用较新的特性，在必要处必须增加非线性动画，使用玻璃元素，使用高级色调
3. 应用程序有五个子页面 —— 日志、注意、倒数、通知、我的
4. 我的页面，支持个人信息、头像和用户名修改，并只读展示当前账号的登录平台
5. 日志页面，支持所有用户上传内容（标题、副标题可选、内容（支持Markdown、Mermaid、Latex）、附件图片（压缩至可接受范围））和评论（在任意帖子下点赞、留言（标题、内容（支持Markdown、Mermaid、Latex）））以及对评论的评论（@形式，其余与评论一致）
6. 注意页面，支持所有用户上传警告或注意内容（标题、内容（Markdown、Latex、Mermaid）、附件图片）
7. 倒数页面，支持所有用户上传倒计时内容，UI显示要高级，支持小组件
8. 通知页面，支持向当前用户传输被点赞、被评论信息
9. 支持本地化，所有的文字需要单独存放

后端至少需要实现以下功能：

使用Python等写后端、支持完善的数据库，支持binlog，支持实时刷新所有已经注册的应用程序的数据，支持推送

---

## 需求评估

当前文档已经描述了目标平台、五个核心页面和主要内容形态，可以作为产品愿景使用；但它还不足以直接进入开发。以下内容尚未明确，并会影响数据模型、技术选型、合规成本、开发周期和验收标准：

- 家庭空间的创建、成员管理和数据归属边界；
- 首发平台、最低系统版本以及“一套跨平台代码”的允许边界；
- 家庭成员退出、内容删除、账号注销和依法处置机制；
- 实时同步、系统推送与数据库 `binlog` 的确切含义；
- Markdown、Mermaid、LaTeX 和附件的安全及资源限制；
- 倒计时、小组件、通知等能力在不同操作系统上的差异；
- 性能、可用性、数据恢复、安全、隐私和验收指标。

下面的“建议答案”是可用于首版设计的默认基线。其中“封闭家庭空间”“首版内容仅家庭内公开、代码层预留仅自己”以及“仅保留微信/QQ 登录且账号各自独立”已经根据 Review 确认；其他答案仍需产品确认。如后续确认结果不同，应同步修改本文件及相关设计。

## 待确认问题与建议答案

### 一、产品范围与发布策略

#### 1. 产品服务的地区和目标用户是谁？

**建议答案：** 首版面向中国大陆的家庭用户，产品形态是仅受邀家庭成员可访问的封闭家庭空间，不提供面向陌生人的公开社区。默认使用简体中文和中国标准时间（UTC+8），同时从数据层保存 UTC 时间并预留其他地区、语言和时区。涉及微信、QQ、用户生成内容和推送时，按中国大陆隐私保护、内容治理及应用商店规则设计。

#### 2. “日志”“注意”“倒数”内容的可见范围是什么？

**建议答案：** 首版所有内容均为 `家庭内公开`，即当前家庭空间内的所有成员可见，不对互联网或其他家庭开放，产品界面不提供可见性选项。数据模型和代码层预留 `仅自己` 可见性枚举，但首版 API 不允许普通客户端写入该状态。所有列表、搜索、详情、评论和通知都必须在服务端校验家庭成员身份，不能把“公开”解释为全站公开。

#### 3. 家庭空间如何创建，家庭成员如何加入和退出？

**建议答案：** 首个用户创建家庭空间并成为家庭管理员，其他成员只能通过一次性邀请链接或邀请码加入；邀请应有有效期、使用次数和撤销能力。首版一个账号只属于一个家庭，成员退出或被移除后立即失去该家庭全部数据访问权；家庭管理员可管理成员，但不能冒充成员操作。产品不提供全站用户发现、关注、陌生人好友、私信或内容推荐；搜索仅覆盖当前家庭内有权限访问的内容。

#### 4. 首版是否必须同时发布 Android、iOS、macOS 和 Windows？

**建议答案：** 代码架构同时覆盖四端，但分阶段交付：第一阶段发布 Android 和 iOS，第二阶段发布 macOS 和 Windows。四端共用领域模型、网络层、状态管理和大部分 UI；小组件、推送、登录回调、分享等系统能力允许使用原生扩展，不要求绝对 100% 共用代码。

#### 5. 最低支持的操作系统版本是什么？

**建议答案：** 在立项时依据选定框架的稳定支持范围固定版本，并写入发布文档。默认建议 Android 10+、iOS 16+、macOS 13+、Windows 10 22H2+；低于该范围只保证后端兼容，不保证客户端安装和视觉效果。

#### 6. 哪些功能属于首版必须交付（P0）？

**建议答案：** P0 包含微信/QQ 独立账号登录、家庭空间创建与成员邀请、五个页面的基础功能、内容发布与查看、评论/回复/点赞、倒计时、本地化框架、站内通知、系统推送、账号注销、家庭成员管理和基础运维后台。复杂动画、多语言译文、桌面小组件、全文搜索、离线编辑和跨家庭能力列为 P1，避免视觉增强阻塞基本闭环。

### 二、客户端与交互

#### 7. 跨平台技术选型是什么？

**建议答案：** 使用 Flutter（稳定版）开发 Android、iOS、macOS 和 Windows 客户端。采用清晰的分层架构，业务状态与 UI 分离；平台特有能力通过受控的原生插件实现。依赖版本需锁定，升级先经过四端回归测试。

#### 8. “非线性动画”“玻璃元素”“高级色调”如何验收？

**建议答案：** 先产出设计令牌和高保真原型，再开发。非线性动画使用统一的弹簧或缓动曲线，主要用于页面切换、卡片展开、点赞反馈和倒计时状态变化；玻璃效果只用于有足够对比度的导航栏、浮层或重点卡片。必须支持系统“减少动态效果”、深色模式、高对比度和低性能降级，不以牺牲可读性、可访问性或续航为代价。

#### 9. 五个“子页面”是否是底部/侧边一级导航？

**建议答案：** 手机端使用五项底部导航；桌面端根据窗口宽度切换为侧边导航。每个一级页面保留独立导航栈和滚动位置。发布、家庭内搜索、详情、编辑等是二级页面，不占用一级导航项。

#### 10. 是否要求离线使用和多端冲突处理？

**建议答案：** 首版支持列表缓存、草稿本地保存和断网后只读，不承诺完全离线发布。写操作失败时保留草稿并允许重试；编辑冲突采用版本号或 ETag 的乐观锁，冲突时提示用户刷新或另存，禁止静默覆盖较新的服务端数据。

#### 11. 本地化需要支持哪些语言？

**建议答案：** 首版实际交付简体中文，框架同时预留英文。所有界面文案、错误信息、通知模板、权限说明和推送模板使用稳定键值单独存放；用户生成内容不自动翻译。日期、数字、复数、时区和无障碍标签也必须本地化，禁止在业务代码中硬编码展示文案。

### 三、账号、身份与个人资料

#### 12. 微信和 QQ 是同一账号的两种登录方式吗？

**已确认答案：** 不是。微信和 QQ 各自创建并使用独立的 MomenPair 用户，一个用户只允许对应一个第三方登录身份。同一平台身份只能对应一个用户，由 `provider + provider_subject` 唯一约束保证；同时由 `user_id` 唯一约束禁止把第二个身份挂到同一用户。即使微信与 QQ 由同一个自然人持有，两边的家庭关系、内容、通知和个人资料也完全隔离，不提供绑定、迁移或合并。

#### 13. 是否保留手机号登录和短信验证码？

**已确认答案：** 不保留。客户端不提供手机号输入或验证码界面，后端不提供相关 API、数据字段或供应商适配器，也不采集手机号。账号登录只依赖微信或 QQ 的 OAuth/原生 SDK 授权结果。

#### 14. 是否支持增加绑定、换绑、解绑、账号合并或跨平台找回？

**已确认答案：** 均不支持。当前账号的登录平台是只读属性，用户不能把微信账号改成 QQ 账号，也不能把两个账号合并。登录能力丢失时应先使用微信或 QQ 官方找回机制；MomenPair 不通过用户名、家庭成员证明或人工改绑绕过第三方身份校验。已登录用户仍可执行数据导出和账号注销。

#### 15. 用户名是否唯一，可修改多少次？头像有什么限制？

**建议答案：** 将唯一且不可频繁修改的系统 ID 与可重复的展示昵称分开。昵称为 1～30 个 Unicode 字符，30 天内最多修改一次；头像支持 JPEG、PNG、WebP，原图最大 10 MB，服务端重新编码并生成多尺寸缩略图，同时去除 EXIF 元数据。昵称和头像需经过格式、文件安全及法律要求所需的校验，不额外引入公共社区式日常内容审核。

#### 16. 登录会话如何管理？

**建议答案：** 使用短时访问令牌和可轮换的刷新令牌；刷新令牌按设备保存哈希并支持单设备退出、全部设备退出和服务端撤销。客户端令牌存入系统安全存储，不写入普通配置、日志或崩溃报告。高风险操作不只依赖尚未过期的访问令牌。

#### 17. 是否支持账号注销和数据导出？

**建议答案：** 必须支持。注销前展示影响并完成身份验证，进入可撤销冷静期；期满后删除或匿名化个人数据，但依法必须保留的安全审计数据按明确期限隔离保存。提供结构化数据导出申请能力，并在隐私政策中说明处理范围和时限。

### 四、内容、评论与互动

#### 18. 日志、注意和倒数是否共用一套内容模型？

**建议答案：** 共用家庭 ID、作者、可见性、状态、附件、创建/修改时间等基础字段，但为三类内容建立独立业务实体或扩展表。首版可见性固定为 `家庭内公开`，同时在模型中预留 `仅自己`。不要把差异很大的字段全部塞进一张带大量空值的表。统一 API 风格和内容渲染协议即可。

#### 19. 标题、正文、图片和附件的具体限制是什么？

**建议答案：** 日志标题 1～100 字，副标题 0～200 字，正文最多 50,000 字；注意标题 1～100 字，正文最多 20,000 字；评论标题改为可选且最多 100 字，正文 1～10,000 字。每条内容最多 9 张图片，单张原图最大 10 MB。客户端可先压缩以改善体验，但服务端仍必须验证真实 MIME、尺寸和文件头，重新编码并生成缩略图，最终资源上限以服务端为准。

#### 20. “附件图片”是否只支持图片，是否需要其他文件类型？

**建议答案：** 首版只支持 JPEG、PNG、WebP 和非动画 GIF；不支持任意文件、SVG、视频或音频。图片使用对象存储和签名上传，上传完成后由服务端确认归属、扫描、转码并落库，未关联的临时对象定期清理。

#### 21. Markdown、Mermaid、LaTeX 如何安全渲染？

**建议答案：** 数据库存储用户原始文本和渲染协议版本，客户端使用固定版本解析器渲染。禁用原始 HTML、脚本、事件属性、危险 URL 协议和 Mermaid 的外部资源访问；链接默认显示风险提示。对复杂内容设置解析深度、节点数、文本长度和渲染超时，防止 XSS、内存耗尽及界面卡死。服务端也执行独立校验，不能只依赖客户端过滤。

#### 22. 评论回复是无限树，还是有限层级？

**建议答案：** 数据层记录 `root_comment_id` 和 `reply_to_comment_id`，界面只展示两层：一级评论及其回复。更深的对话仍归入根评论，并通过 `@展示昵称` 和被回复用户信息表达关系，避免无限缩进及高成本树查询。`@` 必须引用不可变用户 ID，展示名只用于显示。

#### 23. 点赞能否取消，同一用户能否重复点赞？

**建议答案：** 点赞是可取消的幂等操作，同一用户对同一目标最多一条有效记录，由数据库唯一约束保证。计数可以异步汇总，但用户自己的点赞状态必须准确；接口需支持重试而不重复加数。

#### 24. 内容和评论能否编辑、删除？删除后通知如何处理？

**建议答案：** 作者可编辑和删除自己的内容；编辑后保留修改时间，必要时标记“已编辑”。普通用户侧采用软删除并立即隐藏正文，后台保留有限期审计信息；附件异步清理。父内容删除后，家庭成员也不能再访问其评论。既有通知保留事件记录，但目标不可访问时显示“内容已删除”，不泄露原文。

#### 25. 是否需要草稿、自动保存、置顶和收藏？

**建议答案：** 首版提供本地草稿和自动保存；服务端跨端草稿、置顶及收藏列为 P1。草稿不得触发审核、索引、实时广播或通知。

#### 26. 封闭家庭空间是否需要社区治理和内容审核？

**建议答案：** 首版不建设面向公共社区的推荐、举报、禁言、敏感词机审和人工内容审核流程，因为内容只在受邀家庭成员之间流转。仍需提供作者删除、家庭管理员移除成员、违法内容处置入口及受审计的运维删除能力，以满足安全事件和法定义务。内容状态至少包含草稿、已发布、已删除，并预留审核状态扩展；后续若出现跨家庭分享或全站公开能力，必须先补齐完整的审核、举报、申诉和封禁机制。

### 五、倒计时与小组件

#### 27. 倒计时需要哪些字段和重复规则？

**建议答案：** 至少包含标题、目标时间、事件时区、是否全天、固定为家庭内公开的可见性、封面/色彩主题和提醒设置。首版支持一次性事件，以及按天、周、月、年重复；月末和闰日规则必须明确并测试。服务端保存 IANA 时区标识及 UTC 时间，客户端按事件时区计算，避免旅行或夏令时导致结果变化。

#### 28. 目标时间到达后如何显示？是否允许正计时？

**建议答案：** 用户创建时选择“倒数”或“纪念日”。倒数到期后显示“已到期”及经过时长；纪念日持续显示累计天数。重复事件在当前周期结束后计算下一次目标，计算结果由服务端和客户端使用同一规则验证。

#### 29. 小组件覆盖哪些平台，如何刷新？

**建议答案：** P0 支持 iOS WidgetKit 和 Android App Widgets；macOS 可复用 WidgetKit 的能力则纳入第二阶段，Windows 小组件先不承诺，改用开始菜单/任务栏能力评估后再定。小组件读取客户端同步后的最小化共享数据，不持有长期敏感凭据；刷新频率遵守系统调度，不能承诺秒级实时倒计时。界面可用系统时间轴或本地计时展示，但后台刷新以平台限制为准。

#### 30. 倒计时提醒与系统推送是什么关系？

**建议答案：** 单设备、时间确定的提醒优先使用本地通知；跨端变更和服务端事件使用远程推送。创建、编辑或删除倒计时时，各端同步并重新安排本地提醒。服务端保留幂等提醒任务作为兜底，但避免同一事件产生重复通知。

### 六、通知、实时同步与推送

#### 31. “通知页面”包含哪些事件？

**建议答案：** 首版包含家庭邀请、成员加入/退出、内容被点赞、收到一级评论、评论被回复、被 `@` 和账号安全事件。点赞聚合显示，评论和回复按事件显示。通知记录包含家庭 ID、类型、发起者、目标对象、创建时间、已读时间和可安全展示的快照；不能仅保存一段不可解析的文本。

#### 32. 站内通知、实时刷新和系统推送是否是同一件事？

**建议答案：** 三者分开设计：站内通知是可查询、可已读的持久化数据；实时刷新通过 WebSocket/SSE 告知在线客户端增量拉取；系统推送通过 APNs、Android 厂商通道等唤醒或提醒离线用户。推送只携带最少信息和不透明对象 ID，打开后再鉴权拉取详情。

#### 33. “实时刷新所有已经注册的应用程序的数据”具体要求是什么？

**建议答案：** 解释为：同一账号的在线设备，以及同一家庭内有权访问该对象的在线成员，在变更提交后 3 秒内收到失效/增量事件；客户端随后调用 API 获取最新数据。事件只能发送给对应家庭，不能广播给其他家庭或所有注册应用，也不要求离线设备保持实时连接。事件必须带家庭 ID、对象 ID、版本和事件 ID，客户端按事件 ID 去重，并在断线重连后进行增量补偿或全量校准。

#### 34. 推送需要支持哪些渠道？

**建议答案：** iOS/macOS 使用 APNs；Android 在中国大陆采用可替换的聚合层接入主流厂商通道，并视发布地区决定是否接入 FCM；Windows 使用 WNS 或仅提供应用内通知，需在桌面阶段确认。设备令牌与用户、应用版本、平台、环境关联，失效令牌自动停用。用户可按通知类型分别关闭系统推送，但账号安全通知不允许在站内关闭。

#### 35. 通知是否需要精准一次送达？

**建议答案：** 不承诺远程推送“恰好一次”，采用至少一次事件处理、幂等写入和客户端去重。站内通知是事实来源，推送只是尽力提醒。关键事件需通过站内未读列表补偿，不以第三方推送回执作为业务完成依据。

### 七、后端、数据库与数据一致性

#### 36. Python 后端采用什么架构？

**建议答案：** 首版使用 Python 3 的 FastAPI 构建“模块化单体”，按账号、家庭、内容、互动、倒计时、通知和媒体拆分领域模块；异步任务使用独立 Worker。先避免微服务带来的分布式复杂度，当团队规模、流量或隔离需求达到明确阈值后再拆分。

#### 37. “完善的数据库”具体指什么，选择 MySQL 还是 PostgreSQL？

**建议答案：** 如果 `binlog` 是字面上的硬性要求，默认采用 MySQL 8.4 LTS（InnoDB、严格模式、utf8mb4）作为主数据库，Redis 用于缓存、限流和短期状态，对象存储保存图片。搜索早期可使用数据库能力，达到数据量或查询复杂度阈值后再引入专用搜索引擎。数据库版本和云服务应在部署设计中固定，不能只写“最新版”。

#### 38. `binlog` 的用途是备份、审计，还是变更数据捕获（CDC）？

**建议答案：** 将其定义为 MySQL 行格式 `binlog`，用于按时间点恢复和 CDC，不直接作为客户端同步协议。数据库变更事件通过 Outbox 表与业务事务一起提交，再由 Worker/MQ 投递，避免仅监听 `binlog` 时丢失业务语义。若最终选择 PostgreSQL，则应把本文所有 `binlog` 表述改为 WAL/逻辑复制，不能混用术语。

#### 39. 如何保证发帖、点赞、通知和实时事件的一致性？

**建议答案：** 核心数据写入和 Outbox 事件在同一数据库事务中完成；异步消费者使用事件 ID 幂等处理，失败可重试并进入死信队列。点赞等高频操作由唯一约束保证正确，缓存和聚合计数允许最终一致。禁止在数据库事务尚未提交时向客户端广播成功事件。

#### 40. API 如何设计和演进？

**建议答案：** 使用 HTTPS JSON API，并在路径或协议中显式版本化；列表统一使用游标分页，不使用在数据变化时不稳定的深 offset。写接口支持幂等键，资源带版本号，错误返回稳定的机器码和本地化参数。维护 OpenAPI 契约并为兼容性做自动化测试。

#### 41. 实时连接采用 WebSocket 还是 SSE？

**建议答案：** 客户端主要只需接收事件时优先 SSE，以降低协议复杂度；如果后续需要双向在线状态或协作，再升级为 WebSocket。无论选择哪种方式，都要支持鉴权过期、心跳、指数退避、事件游标、断线补偿和连接数保护。具体方案应经过移动网络及桌面休眠恢复测试后锁定。

#### 42. 是否需要管理后台？

**建议答案：** 需要一个基础运维后台，并属于上线 P0。后台至少提供用户与家庭查询、邀请和成员关系排查、账号禁用/恢复、依法处置内容、通知模板、敏感操作审计和基础运行指标；不建设公共社区所需的推荐或日常内容审核后台。管理角色采用最小权限和多因素认证，后台接口与普通用户接口隔离，所有读取或修改家庭数据的行为必须记录原因并留审计记录。

### 八、安全、隐私与合规

#### 43. 需要满足哪些基础安全要求？

**建议答案：** 全链路 TLS；访问令牌、刷新令牌、OAuth 临时 code、第三方访问令牌和应用密钥不得明文持久化或写日志；服务端对所有输入、对象归属和可见性做校验；登录、发布、评论、点赞、上传和搜索均限流。依赖需持续扫描，生产密钥使用密钥管理服务，管理操作启用 MFA，关键风险参考 OWASP ASVS/MASVS 建立检查表。

#### 44. 日志和监控可以记录哪些用户数据？

**建议答案：** 默认不记录访问令牌、刷新令牌、OAuth 临时 code、第三方访问令牌、完整第三方身份标识、原始正文和私有图片地址。日志使用请求 ID、用户内部 ID 和脱敏字段；建立指标、链路追踪、错误告警和安全审计的分层体系，并为每类数据定义访问权限及保留期限。

#### 45. 隐私授权如何处理？

**建议答案：** 首次启动展示隐私政策并按需申请权限，不做捆绑授权。相册、相机、通知等权限只在用户触发相关功能时申请；拒绝后仍可使用无关功能。提供隐私设置、当前登录平台查看、第三方授权撤销说明、数据导出和账号注销入口。第三方 SDK 上线前必须盘点其采集字段、目的和初始化时机。

#### 46. 图片是否需要公开 URL？

**建议答案：** 所有家庭内容均按私有数据处理，原图和缩略图不得使用永久公开 URL，通过短时签名 URL 或鉴权代理访问；即使使用 CDN，也必须在访问前校验用户仍属于对应家庭，资源标识不可包含微信 OpenID、QQ OpenID 等第三方身份信息。成员退出、内容删除或可见性变化时要能及时失效授权和缓存。

### 九、非功能指标与验收

#### 47. 性能和容量目标是什么？

**建议答案：** 在没有业务预测前，先以 10 万注册用户、1 万日活、峰值 500 API 请求/秒和 2 万实时连接作为容量设计基线，但不据此过度拆分服务。常用只读 API 的服务端 P95 小于 300 ms、写 API P95 小于 500 ms（不含第三方登录和上传耗时）；首页首屏应使用分页、缓存和骨架屏。上线前用实测结果修订这些数字。

#### 48. 可用性、备份和灾难恢复目标是什么？

**建议答案：** 首版月度 API 可用性目标为 99.9%；数据库每日全量备份结合持续 `binlog` 归档，目标 RPO 不超过 15 分钟、RTO 不超过 2 小时。对象存储启用版本或回收策略。必须定期执行恢复演练；“存在备份文件”不等于备份可恢复。

#### 49. 测试和验收范围是什么？

**建议答案：** 后端覆盖领域单元测试、数据库集成测试、API 契约测试、权限/越权测试和核心链路压测；客户端覆盖状态逻辑、组件、截图/视觉回归及四端冒烟测试。微信/QQ 登录及账号隔离、OAuth state/code 防重放、家庭邀请、成员退出/移除、跨家庭隔离、预留可见性、评论回复、重复点赞、时区/闰日、断线重连、推送去重和账号注销必须有端到端用例。

#### 50. 发布、灰度和回滚如何进行？

**建议答案：** 区分开发、测试、预发布和生产环境，密钥及数据完全隔离。数据库迁移必须向前兼容并可分阶段执行；服务端支持滚动发布和快速回滚，客户端使用功能开关控制未稳定能力。移动端旧版本不可强制即时升级，因此 API 至少兼容当前及前一个主要客户端版本，并预先定义最低可用版本策略。

## 建议的首版技术基线

在上述建议答案得到确认的前提下，可暂定以下实现基线：

- 客户端：Flutter，共享四端业务代码；小组件、推送、三方登录等使用平台原生扩展。
- 后端：Python 3 + FastAPI 模块化单体 + 异步任务 Worker。
- 数据：MySQL 8.4 LTS + Redis + 对象存储；开启行格式 `binlog`，使用 Outbox 保证业务事件可靠投递。
- 同步：HTTPS API 为数据事实来源，SSE（或评估后改用 WebSocket）发送失效/增量事件，断线后按游标补偿。
- 推送：APNs、Android 厂商推送聚合层，桌面端按第二阶段范围接入；站内通知持久化，系统推送仅作提醒。
- 安全：短时访问令牌、轮换刷新令牌、家庭边界权限校验、内容及图片安全处理、全链路审计和分级限流。
- 交付：先完成移动端 P0 闭环和管理后台，再扩展桌面端、小组件及 P1 能力。

在进入数据库表设计、API 设计和 UI 开发前，除已经由 Review 确认的第 2、3、12～14 项外，至少还应确认第 1、4～6、18、27～38、47～50 项；这些问题会直接改变系统边界或验收结果。
